summary: "lumenclip - slideshow generation"
description: "Generate a complete slideshow through individually observable and composable stages."
value:
  modules:
    - id: validate_input
      summary: "Validate generation input"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 0
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.validate-input"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.validate-input"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: resolve_slide_count
      summary: "Resolve slide count"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 1
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.resolve-slide-count"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.resolve-slide-count"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: select_expand_hook
      summary: "Select and expand hook"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 2
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.select-expand-hook"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.select-expand-hook"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: research_hook
      summary: "Research selected hook"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 3
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.research-hook"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.research-hook"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: build_text_prompt
      summary: "Build structured generation prompt"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 4
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.build-text-prompt"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.build-text-prompt"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: generate_slide_text
      summary: "Generate slideshow text"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 5
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.generate-slide-text"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.generate-slide-text"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: retry_text_similarity
      summary: "Retry similar text"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 6
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.retry-text-similarity"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.retry-text-similarity"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: derive_visual_concepts
      summary: "Derive visual concepts"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 7
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.derive-visual-concepts"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.derive-visual-concepts"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: build_image_shortlists
      summary: "Build image shortlists"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 8
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.build-image-shortlists"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.build-image-shortlists"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: select_slide_images
      summary: "Select slide images"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 9
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.select-slide-images"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.select-slide-images"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: assemble_plan
      summary: "Assemble slideshow plan"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 10
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.assemble-plan"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.assemble-plan"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: translate_plan
      summary: "Translate displayed text"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 11
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.translate-plan"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.translate-plan"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.assemble_plan?.output ?? results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: render_store_pngs
      summary: "Render and store PNG slides"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 12
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.render-store-pngs"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.render-store-pngs"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.translate_plan?.output ?? results.assemble_plan?.output ?? results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: render_store_mp4
      summary: "Render and store MP4"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 13
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.render-store-mp4"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.render-store-mp4"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.render_store_pngs?.output ?? results.translate_plan?.output ?? results.assemble_plan?.output ?? results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: validate_output
      summary: "Validate generated output"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 14
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.validate-output"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.validate-output"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.render_store_mp4?.output ?? results.render_store_pngs?.output ?? results.translate_plan?.output ?? results.assemble_plan?.output ?? results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
    - id: finalize_output
      summary: "Finalize generated output"
      skip_if:
        expr: flow_input.start_at && ["slideshow-generation.validate-input","slideshow-generation.resolve-slide-count","slideshow-generation.select-expand-hook","slideshow-generation.research-hook","slideshow-generation.build-text-prompt","slideshow-generation.generate-slide-text","slideshow-generation.retry-text-similarity","slideshow-generation.derive-visual-concepts","slideshow-generation.build-image-shortlists","slideshow-generation.select-slide-images","slideshow-generation.assemble-plan","slideshow-generation.translate-plan","slideshow-generation.render-store-pngs","slideshow-generation.render-store-mp4","slideshow-generation.validate-output","slideshow-generation.finalize-output"].indexOf(flow_input.start_at) > 15
      stop_after_if:
        expr: result.status === "running" || flow_input.stop_after === "slideshow-generation.finalize-output"
        skip_if_stopped: false
      value:
        type: script
        path: f/lumenclip/run_pipeline_stage
        input_transforms:
          stage_id:
            type: static
            value: "slideshow-generation.finalize-output"
          owner_id:
            type: javascript
            expr: flow_input.owner_id
          request_id:
            type: javascript
            expr: flow_input.request_id
          stage_input:
            type: javascript
            expr: "results.validate_output?.output ?? results.render_store_mp4?.output ?? results.render_store_pngs?.output ?? results.translate_plan?.output ?? results.assemble_plan?.output ?? results.select_slide_images?.output ?? results.build_image_shortlists?.output ?? results.derive_visual_concepts?.output ?? results.retry_text_similarity?.output ?? results.generate_slide_text?.output ?? results.build_text_prompt?.output ?? results.research_hook?.output ?? results.select_expand_hook?.output ?? results.resolve_slide_count?.output ?? results.validate_input?.output ?? flow_input.input ?? { automationId: flow_input.automation_id }"
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  order:
    - automation_id
  properties:
    automation_id:
      type: object
      format: dynselect-automation_id
      title: Template
      description: Choose the Lumenclip template to generate from.
  required:
    - automation_id
  x-windmill-dyn-select-lang: bun
  x-windmill-dyn-select-code: "import * as wmill from \"windmill-client\"\n\nexport async function automation_id(filterText = \"\") {\n  const [baseUrlValue, secretValue, ownerIdValue] = await Promise.all([\n    wmill.getVariable(\"f/lumenclip/internal_base_url\"),\n    wmill.getVariable(\"f/lumenclip/shared_secret\"),\n    wmill.getVariable(\"f/lumenclip/default_owner_id\"),\n  ])\n  const baseUrl = required(\"internal_base_url\", baseUrlValue).replace(/\\/$/, \"\")\n  const secret = required(\"shared_secret\", secretValue)\n  const ownerId = required(\"default_owner_id\", ownerIdValue)\n  const response = await fetch(`${baseUrl}/api/internal/windmill/templates`, {\n    method: \"POST\",\n    headers: {\n      authorization: `Bearer ${secret}`,\n      \"content-type\": \"application/json\",\n    },\n    body: JSON.stringify({ ownerId, kind: \"slideshow\" }),\n  })\n  const payload = await response.json()\n  if (!response.ok || !Array.isArray(payload.options)) {\n    throw new Error(payload.error || `Template lookup failed with ${response.status}`)\n  }\n  const query = filterText.trim().toLowerCase()\n  return query\n    ? payload.options.filter((option) => option.label.toLowerCase().includes(query))\n    : payload.options\n}\n\nfunction required(name: string, value: unknown) {\n  const text = typeof value === \"string\" ? value.trim() : \"\"\n  if (!text) throw new Error(`Lumenclip variable ${name} is not configured`)\n  return text\n}"
