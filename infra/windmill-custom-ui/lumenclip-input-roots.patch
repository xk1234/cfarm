diff --git a/frontend/src/lib/components/graph/FlowGraphV2.svelte b/frontend/src/lib/components/graph/FlowGraphV2.svelte
index 0855ac3..a95d1ee 100644
--- a/frontend/src/lib/components/graph/FlowGraphV2.svelte
+++ b/frontend/src/lib/components/graph/FlowGraphV2.svelte
@@ -1041,6 +1041,7 @@
 				showJobStatus,
 				flowHasChanged,
 				chatInputEnabled,
+				hideInputNode: currentInputSchema?.['x-lumenclip-hide-input-node'] === true,
 				additionalAssetsMap: flowGraphAssetsCtx?.val.additionalAssetsMap
 			},
 			untrack(() => effectiveFailureModule),
diff --git a/frontend/src/lib/components/graph/graphBuilder.svelte.ts b/frontend/src/lib/components/graph/graphBuilder.svelte.ts
index 450debb..799a9c2 100644
--- a/frontend/src/lib/components/graph/graphBuilder.svelte.ts
+++ b/frontend/src/lib/components/graph/graphBuilder.svelte.ts
@@ -423,6 +423,7 @@ export function graphBuilder(
 		showJobStatus: boolean
 		flowHasChanged: boolean
 		chatInputEnabled: boolean
+		hideInputNode: boolean
 		additionalAssetsMap?: Record<string, AssetWithAltAccessType[]>
 	},
 	failureModule: FlowModule | undefined,
@@ -562,6 +563,9 @@ export function graphBuilder(
 				disableMoveIds?: string[]
 			}
 		) {
+			if (extra.hideInputNode && (sourceId === 'Input' || targetId === 'Input')) {
+				return
+			}
 			parents[targetId] = [...(parents[targetId] ?? []), sourceId]
 
 			let index: number
@@ -668,7 +672,7 @@ export function graphBuilder(
 		}
 
 		if (simplifiableFlow?.simplifiedFlow !== true) {
-			nodes.push(inputNode)
+			if (!extra.hideInputNode) nodes.push(inputNode)
 			nodes.push(resultNode)
 		}
 
@@ -1204,6 +1208,15 @@ export function graphBuilder(
 
 		if (simplifiableFlow?.simplifiedFlow === true && triggerNode) {
 			processModules(topLevelItems, undefined, triggerNode, undefined, true, undefined)
+		} else if (extra.hideInputNode) {
+			processModules(
+				topLevelItems,
+				undefined,
+				preprocessorModule ?? triggerNode ?? inputNode,
+				resultNode,
+				false,
+				undefined
+			)
 		} else {
 			processModules(topLevelItems, undefined, inputNode, resultNode, false, undefined)
 		}
