:8000 {
	@backend path /api /api/* /ws /ws/* /metrics /metrics/* /.well-known /.well-known/* /mcp /mcp/* /health /ready
	handle @backend {
		reverse_proxy 127.0.0.1:8001
	}

	handle {
		root * /static_frontend
		try_files {path} /200.html
		file_server
	}
}
