---
title: "Slideshow automation templates"
description: "Browse slideshow template examples, automation directions, per-slide content requirements, and canonical JSON settings."
full: true
---

import { SlideshowAutomationTemplateAssets } from "@/components/docs/automation-template-assets"

Every catalog template is a reusable starting point rather than a shared live
automation. Selecting **Use** in the app creates an owned copy that can be
edited without changing this definition.

The example browser uses curated runs already attached to each template. If a
template has no stored example slideshow, the example section is omitted.

<SlideshowAutomationTemplateAssets />
